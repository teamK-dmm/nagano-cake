<div class="container">
  <div class="row mt-5">
    <div class="col-2">
      <table class="table table-bordered">
        <thead>
          <tr >
            <th class="text-center">ジャンル</th>
          </tr>
        </thead>
        <tbody>
          <% @genres.each do |genre| %>
            <tr>
              <td><%= genre.name %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div class="col-10">
      <section class="main-visual mt-4">
        <div class="mv-text">
          <p class=text-center>ようこそ、ながのCAKEへ！</p><br>
          <p class=text-center>このサイトは、ケーキ販売のECサイトになります。</p><br>
          <p class=text-center>会員でない方も商品を閲覧できますが、</p><br>
          <p class=text-center>購入には会員登録が必要になります。</p><br>
        <div>
      <section>
    </div>
  </div>

  <div class="row mt-4">
    <section class="new-items col-md-12">
      <h3><strong>新着商品</strong></h3>
      <div class="row">
        <% @items.each do |item| %>
          <div class="col-3">
            <div class="card border border-0">
              <%= link_to public_item_path(item.id) do %>
                <%= image_tag item.get_image(100,100) %><br>
              <% end %>
              <div class="card-body">
                <h6 class="card-title"><strong><%= item.name %></strong></h6>
                <p class="card-text">￥<%= item.price %></p>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </section>
  </div>
  <div class="col-md-12 text-right">
    <%= link_to "全ての商品を見る>", public_items_path %>
  </div>
</div>